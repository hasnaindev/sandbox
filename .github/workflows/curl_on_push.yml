name: "Basic cURL Action"
on:
  push:
  workflow_dispatch:

jobs:
  basic-curl:
    runs-on: ubuntu-latest
    steps:
      - name: "Basic cURL"
        run: curl -s https://jsonplaceholder.typicode.com/todos/1 | jq
